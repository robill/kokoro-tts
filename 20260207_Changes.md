# Changes made on 2026-02-07

## 1. Batch File Enhancements
- Created `run_tts_split_merge.bat` to automate both splitting audio into chunks and merging them into chapters.
- Added logic to use the default voice or a specific blend (e.g., `--voice "af_nicole:70,bm_lewis:30"`).
- Improved the batch file to:
  - Only run the merge step if no input file is specified.
  - Remove the `--voice` parameter from the merge operation, as it is not needed.

## 2. Audio File Recovery and Output
- Explained how temporary audio files are handled:
  - With `--split-output`, partial progress is recoverable.
  - Without `--split-output`, no partial files are saved.
- Described how to merge split audio files using the application's `--merge-chunks` option.

## 3. Merging MP3 Chapters
- Provided a Python script (`merge_chapters.py`) to merge multiple chapter MP3 files into a single MP3 file.
- Enhanced the script to:
  - Autodetect MP3 files in a given folder (passed as a command-line argument).
  - Sort files in natural order (e.g., 01, 02, ..., 10, 11, ...).
  - Print the order of files to be merged for verification.

## 4. Troubleshooting and Usage
- Provided guidance on running Python scripts and activating virtual environments in PowerShell (including handling execution policy errors).
- Explained how to run the merge script and how to specify the folder containing chapter files.
- Helped troubleshoot and correct file sorting issues in the merge script.

---

These changes and instructions were made to improve workflow, automate audio processing, and ensure correct merging of chapter files.
